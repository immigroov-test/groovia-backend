# Claude Code internal files
.claude/

# Secrets - never commit. Ignore all .env* except the placeholder template.
.env*
!.env.example
*.key
*Auth keys.txt
*auth keys.txt
secrets.json
Meeting_1_feedback.txt

# Third-party reference code - never push
immigroov-main/

# Frontend is now its own repo (immigroov-test/groovia-frontend); never track it here.
groovia-frontend/

# Python
__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Local data & generated artifacts
data/
etc/
*visualization.png
graph.png
system_architecture.png
_dev_emails.jsonl
# Legacy mentor migration downloads - contain PII, NEVER commit
mentor_migration_raw.json
mentor_migration_preview.json

# Node (none here now; harmless)
node_modules/
.next/
.turbo/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local DR backups (never commit - may contain PII)
scripts/backups/
scripts/*.log

# Local-only: production setup runbook (contains the launch checklist, kept off git)
docs/PRODUCTION_SETUP.md

# Generated prod seed dumps (contain mentor PII, regenerate with scripts/export_prod_seed.py)
scripts/prod_seed.sql
scripts/prod_seed/

# Legacy AWS database archive: production customer PII (emails, phones, payments).
# Never commit, wherever it is placed.
legacy-archive/
immigroov-legacy-archive/
*.parquet
